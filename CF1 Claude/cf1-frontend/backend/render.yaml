services:
  - type: web
    name: cf1-backend
    env: node
    buildCommand: npm install && npm run build || npm install
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: FRONTEND_URL
        value: https://rwa2.netlify.app